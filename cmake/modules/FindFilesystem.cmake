#find_library(FILESYSTEM_LIBRARY NAMES stdc++fs)

#set(FILESYSTEM_LIBRARIES ${FILESYSTEM_LIBRARY} )

#message(FILESYSTEM_LIBRARIES = ${FILESYSTEM_LIBRARIES})

#include(FindPackageHandleStandardArgs)
#find_package_handle_standard_args(Filesystem DEFAULT_MSG FILESYSTEM_LIBRARIES)

#mark_as_advanced(FILESYSTEM_LIBRARIES)
